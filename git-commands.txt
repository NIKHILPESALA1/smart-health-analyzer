# Copy and paste these commands one by one in your terminal
# Make sure you're in the ai-health-analyzer folder first

# Step 1: Initialize git
git init

# Step 2: Add all files
git add .

# Step 3: Make first commit
git commit -m "Initial commit: Smart Health Analyzer - AI-powered symptom checker"

# Step 4: Add your GitHub repository as remote
# Replace YOUR_USERNAME with your actual GitHub username
git remote add origin https://github.com/YOUR_USERNAME/smart-health-analyzer.git

# Step 5: Rename branch to main
git branch -M main

# Step 6: Push to GitHub
git push -u origin main

# Done! Your code is now on GitHub 🎉
